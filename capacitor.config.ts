import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'oliveira.luan.videosapp',
  appName: 'videosApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
